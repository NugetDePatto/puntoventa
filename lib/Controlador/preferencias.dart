import 'package:flutter/material.dart';

class Pref {
  Color colorAppBar = Color(0xFF1F1B24);
  Color colorBotones = Color(0xFF343039);
  Color colorBackground = Color(0xFF545058);
  // Color colorBotones = Colors.blue;
  // Color colorBackground = Colors.white;
}
